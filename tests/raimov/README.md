# RAIMOV tests

Reserved for project-scoped e2e, accessibility, forms, security, release, visual, redirect and helper tests.

Recommended split: `e2e/`, `axe/`, `forms/`, `security/`, `release/`, `visual/`, `redirects/`, `helpers/`.

The existing `tests/raimovdental/` suite remains active. Consolidation or renaming requires a separate migration with command parity and CI evidence.

# Evidence folder

Commit 1 stores **probe metadata only** (`http-probes.csv`).

Raw HTML/screenshots may be added in Commit 2 if:

- publicly accessible,
- no private/patient content,
- size/rights acceptable,
- linked from SOURCE/FACT rows with UTC timestamp.

Do not treat saved HTML as clinic confirmation.

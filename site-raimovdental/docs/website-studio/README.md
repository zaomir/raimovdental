# Website Studio change manifests — RAIMOV DENTAL

The project-wide manifest for the single Stage A route is:

- `site-raimovdental/docs/QA_MANIFEST.md`

Stage A intentionally contains one protected/noindex representative route: `/stage-a/`. A route-level manifest is added only if a future approved route needs distinct tests or release conditions.

Source templates remain under `docs/templates/website-studio/`. The governing standard is `docs/ssot/WEBSITE_STUDIO_STANDARD.md` and decision `DEC-737`.

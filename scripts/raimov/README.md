# RAIMOV scripts

Reserved for project-scoped gates, generators, reports, migrations and shared helpers.

Recommended split: `gates/`, `generators/`, `reports/`, `migration/`, `lib/`.

Existing production commands remain authoritative until a separate migration is approved. Scripts must fail closed on missing evidence, unsafe publication state or secret exposure.

(()=>{'use strict';const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;function animate(node,cls='motion-enter'){if(reduced||!node)return;node.classList.remove(cls);void node.offsetWidth;node.classList.add(cls);setTimeout(()=>node.classList.remove(cls),450)}function markSelected(target){const option=target.closest('.quiz-option,.btn');if(!option)return;option.classList.add('motion-selected');setTimeout(()=>option.classList.remove('motion-selected'),360)}document.addEventListener('click',event=>{markSelected(event.target);const button=event.target.closest('button');if(button&&!reduced){button.style.pointerEvents='none';setTimeout(()=>{button.style.pointerEvents=''},220)}},true);const observer=new MutationObserver(records=>{const seen=new Set();for(const record of records){for(const node of record.addedNodes){if(node.nodeType!==1)continue;const target=node.matches?.('.panel,.card')?node:node.querySelector?.('.panel,.card');if(target&&!seen.has(target)){seen.add(target);animate(target)}}}if(records.some(record=>record.target.id==='main'||record.target.id==='learnBody')){animate(document.querySelector('#main > .panel')||document.querySelector('#learnBody > *'),'motion-fade')}});function start(){const main=document.getElementById('main');if(main)observer.observe(main,{childList:true,subtree:true});const app=document.getElementById('app');if(app)observer.observe(app,{childList:true,subtree:true});animate(document.querySelector('.login-card'));}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',start,{once:true}):start();})();